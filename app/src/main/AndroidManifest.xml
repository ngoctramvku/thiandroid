<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vku.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <meta-data
        android:name="android.app.default_searchable"
        android:value="com.apkgetter.SearchResultsActivity" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".Register_Activity"></activity>
        <activity android:name=".DanhmucActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".ProductGrid" />
        <activity android:name=".FashionGrid" />
        <activity android:name=".FoodGrid" />
        <activity android:name=".ProductActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".Fashion_Activity" />
        <activity android:name=".Food_Activity" />
        <activity android:name=".DetailOrderActivity" />
    </application>

</manifest>